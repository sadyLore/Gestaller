<?xml version="1.0" encoding="utf-8"?>
<menu xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto">

    <group android:checkableBehavior="single">

        <!-- Visible para propietario y colaborador -->
        <item
            android:id="@+id/nav_clientes"
            android:title="Clientes" />

        <!-- Solo propietario (se ocultan en EmployeeHomeActivity) -->
        <item
            android:id="@+id/nav_vehiculos"
            android:title="Vehículos" />

        <item
            android:id="@+id/nav_servicios"
            android:title="Servicios" />

        <!-- Visible para ambos -->
        <item
            android:id="@+id/nav_trabajos"
            android:title="Trabajos" />
    </group>

    <item
        android:id="@+id/nav_theme_toggle"
        android:title="Cambiar tema" />
</menu>


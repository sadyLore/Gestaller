<resources xmlns:tools="http://schemas.android.com/tools">

    <style name="AppTheme" parent="Theme.MaterialComponents.DayNight.NoActionBar">
        <item name="android:fontFamily">@font/sans_serif_medium</item>
        <item name="android:colorBackground">@color/white</item>
        <item name="colorPrimary">@color/gestaller_blue</item>
        <item name="colorAccent">@color/gestaller_blue</item>
        <item name="android:statusBarColor">@color/gestaller_blue</item>
    </style>

    <!-- Estilo para botones principales -->
    <style name="GestallerButton">
        <item name="android:layout_width">match_parent</item>
        <item name="android:layout_height">wrap_content</item>
        <item name="android:backgroundTint">@color/gestaller_blue</item>
        <item name="android:textColor">@color/white</item>
        <item name="android:fontFamily">@font/sans_serif_medium</item>
        <item name="android:textSize">16sp</item>
        <item name="android:padding">14dp</item>
        <item name="android:layout_marginTop">14dp</item>
        <item name="android:layout_marginHorizontal">30dp</item>
        <item name="android:background">@drawable/rounded_button</item>
    </style>

    <!-- Estilo para los textos del Drawer -->
    <style name="DrawerItemText">
        <item name="android:textSize">18sp</item>
        <item name="android:textColor">@color/black</item>
        <item name="android:fontFamily">@font/sans_serif_medium</item>
        <item name="android:gravity">start</item>
        <item name="android:textAlignment">viewStart</item>
    </style>

</resources>
